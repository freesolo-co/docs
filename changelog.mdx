---
title: "Changelog"
description: "New models, faster training and serving, and platform updates from the Freesolo team."
rss: true
---

<Update
  label="July 31, 2026"
  description="2026-07-31"
  tags={["Flash CLI", "Environments"]}
  rss={{
    title: "July 31, 2026",
    description: "Evaluate a deployed model against held-out suites, and gate scripts on a deploy.",
  }}
>
  ## Featured

### Evaluate a deployed model against held-out suites

Put an `evaluations.py` beside your `environment.py` and `flash env eval RUN_ID`
scores those suites against the deployed adapter. Cases that never reached the
model are counted as errors instead of averaged in as zeros, so a half-broken
deployment reads as broken rather than as a weak model.

### Gate a script on a deploy instead of polling

`flash models deploy --wait` blocks until the revision is actually servable and
exits non-zero if it fails, so a deploy can be chained in automation. Available
in Flash 1.0.34.

  <Accordion title="Improvements" description="4 items">
    - **Environments:** `flash env test` can drive the split you actually train on
      with `--split` and `--param`, and now fails when every replayed gold answer
      scores zero.
    - **Environments:** Scope an eval with `--suite`, cap it with `--max-cases`, and
      raise `--concurrency` to run cases in parallel.
    - **Flash CLI:** `flash train --dry-run` checks your org balance against the
      quote, so an insufficient balance fails before you submit.
    - **Flash CLI:** `flash env setup` scaffolds a starter `evaluations.py`.
  </Accordion>
</Update>

<Update
  label="July 28, 2026"
  description="2026-07-28"
  tags={["Platform", "Tracing", "Serving"]}
  rss={{
    title: "July 28, 2026",
    description: "Turn your app's live traffic into a training set, and cleaner output from thinking models.",
  }}
>
  ## Featured

### Turn your app's live traffic into a training set

Point an OpenAI-compatible client at `https://api.freesolo.co/v1` and Freesolo
records your app's chat completions against a project, then hands them back as a
training dataset with `flash traces export`. Secrets are redacted before anything
is forwarded or stored. See [Tracing](/guides/tracing).

### Thinking models split reasoning out of `content`

Reasoning now comes back in `reasoning_content` and only the answer in `content`,
in both JSON and streaming responses, so the `</think>` separator no longer leaks
into your output.

  <Accordion title="Improvements" description="1 item">
    - **Serving:** `flash models chat` folds the two fields back into one
      `<think>...</think>` block, so the CLI still shows the full generation.
  </Accordion>
</Update>

<Update
  label="July 27, 2026"
  description="2026-07-27"
  tags={["Flash CLI", "Platform", "Projects"]}
  rss={{
    title: "July 27, 2026",
    description: "Every run and environment belongs to a project, and run and serving commands are grouped.",
  }}
>
  ## Featured

### Every run and environment belongs to a project

Training configs take a required top-level `project` UUID, and `flash env push`
and `flash env delete` take a required `--project`. Flash validates the project
against your organization before allocating a GPU, so a wrong or missing project
fails the submit instead of starting paid work. Available in Flash 1.0.25.

### Run and serving commands are grouped

`flash status`, `log`, `cancel`, and `checkpoints` moved under `flash runs`.
`flash deploy`, `chat`, `undeploy`, `deployments`, and `export` moved under
`flash models`. Listing base models is `flash models list`.

  <Accordion title="Improvements" description="3 items">
    - **Platform:** Deploy and tear down serving from the dashboard, without
      switching to the CLI.
    - **Platform:** A project selector in the top bar moves between projects from
      anywhere in the dashboard.
    - **Flash CLI:** Create and find projects with `flash projects create` and
      `flash projects list`.
  </Accordion>
</Update>

<Update
  label="July 24, 2026"
  description="2026-07-24"
  tags={["Platform", "Projects"]}
  rss={{
    title: "July 24, 2026",
    description: "Organize runs, environments, and traces into projects.",
  }}
>
  ## Featured

### Organize runs, environments, and traces into projects

Group related work under an organization-level project and switch between
projects from the platform.

</Update>

<Update
  label="July 22, 2026"
  description="2026-07-22"
  tags={["Platform", "Training"]}
  rss={{
    title: "July 22, 2026",
    description: "Watch training as it happens with live metrics, logs, and sample completions.",
  }}
>
  ## Featured

### Watch training as it happens

Live metrics, run logs, and sample completions in the platform, plus per-step
rewards from the Flash CLI.

</Update>

<Update
  label="July 21, 2026"
  description="2026-07-21"
  tags={["Serving", "Training"]}
  rss={{
    title: "July 21, 2026",
    description: "7-10x faster generation on 9B and 27B models, and SFT training up to 48% faster.",
  }}
>
  ## Featured

### 7-10x faster generation on 9B and 27B models

### SFT training up to 48% faster

Lower memory use and faster data processing also unlock longer training contexts.

  <Accordion title="Improvements" description="1 item">
    - **Training:** Multi-turn GRPO tasks with independently gradable turns can
      choose per-turn or per-episode credit.
  </Accordion>
</Update>

<Update
  label="July 20, 2026"
  description="2026-07-20"
  tags={["Models"]}
  rss={{
    title: "July 20, 2026",
    description: "Train and serve multimodal models with image inputs.",
  }}
>
  ## Featured

### Train and serve multimodal models

Use image inputs with multimodal SFT, GRPO, on-policy distillation, and chat
serving.

</Update>

<Update
  label="July 19, 2026"
  description="2026-07-19"
  tags={["Models"]}
  rss={{
    title: "July 19, 2026",
    description: "Qwen3.6-27B is now available to train and serve.",
  }}
>
  ## Featured

### Qwen3.6-27B is now available

Train and serve Qwen3.6-27B on Freesolo.

</Update>

<Update
  label="July 17, 2026"
  description="2026-07-17"
  tags={["Serving", "Training"]}
  rss={{
    title: "July 17, 2026",
    description: "32k context windows for training and serving.",
  }}
>
  ## Featured

### 32k context for training and serving

Train and serve supported models with context windows up to 32k tokens.

</Update>
